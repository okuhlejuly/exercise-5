const numbers = [1, 2, 3, 4, 5]

for (number of numbers) {
    console.log(number);
}

i = 5

while (i > 0) {
    console.log(i)
    i -= 1
}

for (number of numbers) {
    if (number % 2 === 0) {
        console.log(number);
    }    
}

let sum = 0;

for (number of numbers) {
    sum += number;
}

console.log(sum)